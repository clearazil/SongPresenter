{% extends 'base.html.twig' %}
{% block body %}

<div class="col-md-8">
        {% if (success) %}
            <p>{% trans %}PASSWORD_EMAIL_SENT{% endtrans %}</p>
        {% endif %}
    <!-- BEGIN: CONTENT/USER/LOGIN-FORM -->
        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ form_errors(form) }}
        {{ form_row(form.email) }}
        {{ form_end(form) }}
    <!-- END: CONTENT/USER/LOGIN-FORM -->
</div>

{% endblock %}